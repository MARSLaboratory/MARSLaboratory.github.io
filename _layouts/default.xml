<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ page.title }} | {{ site.title }}</title>
  <style>
    body { font-family: system-ui, sans-serif; max-width: 760px; margin: 40px auto; line-height: 1.6; color: #222; }
    header { border-bottom: 1px solid #ddd; margin-bottom: 30px; padding-bottom: 10px; }
    nav a { margin-right: 15px; text-decoration: none; color: #0366d6; }
    nav a:hover { text-decoration: underline; }
    footer { border-top: 1px solid #ddd; margin-top: 40px; padding-top: 10px; font-size: 0.9em; color: #666; }
    h1, h2, h3 { color: #111; }
  </style>
</head>
<body>
  <header>
    <h1>{{ site.title }}</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/about.html">About</a>
    </nav>
  </header>

  <main>
    {{ content }}
  </main>

  <footer>
    <p>&copy; {{ site.title }} — {{ site.description }}</p>
  </footer>
</body>
</html>
